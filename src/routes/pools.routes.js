import { Router } from "express";

const router = Router();

router.post("/pool", () => {});
router.get("/pool", () => {});
router.get("/pool/:id/choice", () => {});
router.get("/pool/:id/result", () => {});

export default router;